import psutil

def proc_exist(process_name): #判断程序是否运行，返回pid
    pl = psutil.pids()
    for pid in pl:
        if psutil.Process(pid).name() == process_name:
            return pid

if isinstance(proc_exist('chrome.exe'),int):
    print('chrome is run')
else:
    print('no is run')
